/* Copy this file to assets/secrets.js and paste your OpenRouter key.
   assets/secrets.js is gitignored, so the key never reaches the repo.

   WARNING: in a static client-side site the key is still visible to anyone
   who opens this page in a browser. For a real public deploy, use a
   server-side proxy (e.g. a Cloudflare Worker) that holds the key instead. */
window.ROAST_KEY = ""; // paste your OpenRouter key here for LOCAL dev only
